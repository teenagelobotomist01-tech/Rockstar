<h1 class="form-title">Registrarse</h1>

<% if @user.errors.any? %>
  <div class="form-errors">
    <h2><%= pluralize(@user.errors.count, "error") %> impidieron el registro:</h2>
    <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>


<%= form_with model: @user, url: register_path, local: true, html: { class: "auth-form" } do |form| %>
  <div class="form-group">
    <%= form.label :name, "Nombre" %>
    <%= form.text_field :name, class: "form-input" %>
  </div>

  <div class="form-group">
    <%= form.label :email, "Correo electrónico" %>
    <%= form.email_field :email, class: "form-input" %>
  </div>

  <div class="form-group">
    <%= form.label :password, "Contraseña" %>
    <%= form.password_field :password, class: "form-input" %>
  </div>

  <div class="form-group">
    <%= form.label :password_confirmation, "Confirmar contraseña" %>
    <%= form.password_field :password_confirmation, class: "form-input" %>
  </div>

  

  <%= form.submit "Registrarme", class: "form-submit" %>
<% end %>

  <div class="form-group">
    <%= link_to "ya tengo una cuenta", login_path, class: "enlace-derecha" %>
  </div>
